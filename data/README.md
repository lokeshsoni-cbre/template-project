# Data Directory

This directory is gitignored. Create subdirectories as needed:

- `raw/` — Original, immutable data
- `processed/` — Cleaned, transformed data
- `external/` — Data from third-party sources

Never commit real data files to git.
